#!/bin/sh
# sketchybar-app-font has branded glyphs for the common macOS apps.
# The shared map covers Slack, Telegram, Finder, terminals, browsers, IDEs,
# communication, media, and system apps, with :default: as a fallback.
exec "$HOME/.config/sketchybar/themes/rice-reference/plugins/icon_map_fn.sh" "$1"
